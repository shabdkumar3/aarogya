# Core (Gradio 5 to fix HfFolder import error)
gradio==5.9.1
huggingface_hub>=0.26.0
python-dotenv>=1.0.1
Pillow>=10.4.0
requests>=2.32.3

# Voice — STT (faster-whisper works on Python 3.11+, openai-whisper does not)
faster-whisper==1.0.3
ffmpeg-python==0.2.0

# Voice — TTS
gTTS==2.5.1

# PDF Reports
reportlab==4.2.2

# Optional: REST API (uncomment only if running api.py for the React frontend)
# fastapi==0.115.0
# uvicorn[standard]==0.30.1
# python-multipart==0.0.9

# Fine-tuning runs separately on Kaggle GPU — see finetuning/AAROGYA_KAGGLE.ipynb
