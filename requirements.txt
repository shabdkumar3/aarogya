# Core
gradio>=5.0.0
huggingface_hub>=0.26.0
python-dotenv>=1.0.1
Pillow>=10.4.0
requests>=2.32.3

# Voice — STT (faster-whisper works on Python 3.11+, openai-whisper does not)
faster-whisper==1.0.3
ffmpeg-python==0.2.0

# Voice — TTS
gTTS==2.5.1

# PDF Reports
reportlab==4.2.2

# Fine-tuning (Kaggle GPU only — see finetuning/AAROGYA_KAGGLE.ipynb)
# unsloth, trl, datasets, transformers, torch, bitsandbytes

# REST API (optional — only if running api.py)
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
